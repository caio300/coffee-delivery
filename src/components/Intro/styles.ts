import styled from 'styled-components'
import ItroBackground from '../../assets/intro-background.svg'

export const IntroContainer = styled.div`
  height: 34rem;
  filter: blur(80px);
  background-image: url('${ItroBackground}');
`
